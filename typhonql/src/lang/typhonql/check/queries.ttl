test E1 [[ from User u select u.name ]]
test EF1 [[ from User u select u.name1 ]] expect { "name1 not defined for `User`" }
test EF2 [[ from User u select u.name where u.name > 3 ]] expect { "Cannot compare `string` and `int`" }